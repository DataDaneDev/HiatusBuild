# Media

Store both progress photos and inspiration in one place to reduce folder sprawl.

Suggested subfolders:
- `media/progress/`
- `media/inspiration/`

For each import, add a short note in `logs/LOG.md` with date and relevance.
